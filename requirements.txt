# Backend
fastapi>=0.100.0
uvicorn>=0.23.0
networkx>=3.0
numpy>=1.24.0
pydantic>=2.0.0
pyyaml
python-multipart
pymongo>=4.6.0

# Graph ML (ml/ — EPSS prediction; see Docs/Plans/GraphML_EPSS_Prediction.md)
# torch is installed from a CUDA index for GPU (Blackwell / RTX 50xx needs cu128):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# The version pin below is satisfied by either the CPU or a CUDA wheel.
scikit-learn>=1.5.0
xgboost>=2.0.0
torch>=2.4.0
torch-geometric>=2.5.0
tensorboard>=2.15.0
matplotlib>=3.7.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
playwright>=1.40.0
pytest-playwright>=0.4.0
mongomock>=4.1.0
