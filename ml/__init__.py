"""Graph-ML package for EPSS prediction over the vulnerability-chain graph.

See Docs/Plans/GraphML_EPSS_Prediction.md for the staged plan. This package
holds the experiment-phase code (D16): a corpus exporter (GML-0) and, later,
label joining + training scripts (GML-1/2). The serving container (GML-3) is
built only after the experiment gate.
"""
